# 需求驱动重建 · V1 第 6—8 章

2026-09-09。旧版本在提交 `3e7bb11` 保存，随后删除网站实现、旧样式、旧图片和旧产品定位。新实现只依据章节计划、通用规范和重新读取的原网站三章内容。

## 当前范围

- 06：N=3 副本状态，R/W 配置、延迟写入、网络隔离、逐步快照、旧值读取、恢复读修复；并发购物车与向量时钟策略对比。
- 07：精确 64 位 Snowflake 编解码、位字段、同毫秒容量、4097 请求预设、时钟回拨保护、重复身份碰撞。
- 08：真实 Base62 编码、教学哈希冲突与重试、复用和自定义别名、映射唯一性；浏览器和服务端缓存、访问计数、未知 / 过期地址、目标变更。
- 每章包含知识地图、概念例子和误区、方案比较、4 道练习、90 秒复述、自评与本地笔记导出。三章切换保留当前实验状态。

## 明确边界

固定副本单写者模型不承诺生产数据库线性一致性；并发实验只模拟添加购物车商品。提示移交、反熵和磁盘路径为独立讲解分镜，没有实现整个存储引擎。

Snowflake 等待溢出请求需要用户推进模拟时间后重新提交；只展示最近 12 个 ID，批量生成仍计算全部 4096 个值。首次发号后机器身份锁定，清空发号状态可重新配置。

短网址为本地教学，不访问目标。缓存预设明确依赖客户端策略；短码目标更新是原书不可修改假设之外的对照实验。哈希为确定性教学函数，不冒充 MD5/SHA。原文存储容量的量纲错误在页面内校正。

## 验证与发布

运行 `npm run build` 生成 GitHub Pages 的 `docs/`。运行 `node scripts/verify.mjs` 验证关键机制与浏览器流程；可通过 TEST_URL 指向线上同一部署。脚本会在有效页面加载后生成桌面和手机截图。

仓库沿用 `holynova/system-design-learning-lab`，分支 `master`，Pages `/docs`。保留用户指定的 Umami ID、源码链接、简洁 README 与 QR。
