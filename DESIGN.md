# ARC / LAB 视觉系统

ARC / LAB 把系统设计学习做成一张可以操作的架构工作台。页面的核心动词是“预测、运行、解释”：学习者先看到章节语境，再沿着请求路径逐步推进，最后用思维导图和面试题复述取舍。

## Visual world

- **Surface**：温和的纸张灰作为背景，深墨蓝承载结构与主要文字，珊瑚色只标记当前动作，薄荷绿表示已经通过的状态，金色标记章节序号。
- **Typography**：IBM Plex Sans 负责中文与正文，DM Mono 负责章节编号、状态、假设和事件标签。大标题保持紧凑，实验中的编号和状态保持等宽，形成“编辑桌面 + 工程仪表”的关系。
- **Shape**：直角纸张面板、细边框、轻阴影和少量圆角。层级来自留白、边界和状态色，而不是渐变或装饰插图。
- **Motion**：只在路径推进、当前节点放大、按钮悬停和控制值变化时反馈；交互始终保留静止状态，移动端可读。

## Layout and narrative

固定左侧章节导航让内容范围随时可见；顶部栏提供原书目录和 GitHub 入口。首屏按“章节标题 → 架构实验 → 下一步”排列，首个操作不需要滚动。限流器和一致性哈希有专属实验面板，其余章节使用轻量的事件追踪模型，保持 28 章的一致入口。

每个章节共享三种学习视图：架构实验用于操作和观察，思维导图用于建立概念网络，快速复习用于检查表达。架构画布内的虚线边界和连接线是测量与路径工具，不是页面背景装饰。

## Responsive behavior

桌面端保留章节导航与双列实验布局；窄屏隐藏侧栏、保留移动品牌和顶部入口，所有实验、思维导图和练习纵向堆叠。按钮保持可触达的高度，颜色对比和 `:focus-visible` 轮廓服务键盘与辅助技术使用。

## Shipped states

- 默认打开第一卷第 4 章“设计一个限流器”，让首屏直接展示有专属参数面板的深度实验。
- 每章均有 4 个左右的架构节点、事件解释、思维导图和一道练习题；第 4、5 章额外提供算法、速率、突发量、节点和虚拟节点控制。
- `v0.1.0` 是内容模型版本；页面通过 Umami website ID `e01c9f78-4607-4e60-b01c-77c8190b12b4` 记录核心点击事件。
