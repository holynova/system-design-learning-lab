# 通用学习体验与内容规范

## 一章的学习路径

建议每章拆成可以中断恢复的学习单元：先看 2 分钟问题卡，再读原文对应段落，完成一个 5—10 分钟实验，展开思维导图，最后做 3—5 题并复述。大章拆成两到三个单元，不强迫一次读完。

每章顶部明确三个问题：它解决什么问题？本章暂不解决什么问题？学完应该能解释什么？用具体动作定义目标，例如“解释为什么消费者增加到分区数以上后没有继续提高并行度”，不用“掌握消息队列”等模糊表述。

页面提供原文入口、先修知识、核心实验、思维导图、练习与复习入口。原文对应采用章节内小节名称；不依赖未经核对的锚点。用户切换模块时保留实验位置和笔记。

## 每项实验的共同交互契约

1. **先给问题。** 开始前展示初始数据和一个预测题；可跳过，但不会被默认为答对。
2. **画清系统。** 节点可点击查看职责、输入、输出和状态；连线标出方向、同步/异步及传递数据。点击应有键盘等价操作。
3. **控制进度。** 开始、暂停、下一步、上一步查看快照、重置；只在用户启动后播放。时间敏感实验使用明确的模拟时间，不受设备运行快慢影响。
4. **每步有证据。** 高亮相关节点，列出事件编号、状态前后差异和一句解释。不能仅靠彩色流光表达成功或失败。
5. **有界的参数。** 章节计划给出建议范围与样例。影响历史结果的参数变更时，提示“重新运行此场景”，不悄悄修改已发生事件。
6. **故障可以恢复。** 除注入故障，还应能查看排队、重试、恢复后数据是否正确；重置恢复全部初始状态。
7. **可以公平比较。** 两个方案共享事件序列、参数口径与随机种子；显示差异来自哪里。无依据时只呈现操作次数、状态变化，不生成虚假的毫秒性能。
8. **最后解释。** 展示预测和实际结果，要求填写“现象—原因—代价”；允许保留不同的合理方案。

低动态模式用状态快照和文字事件表替代动画。手机将画布切换为组件列表和事件时间线；复杂拖拽同时提供选择框或按钮。所有数值图表都有文字摘要，信息不只通过颜色区分。

## 思维导图规范

每章计划中的“思维导图”给出实际一级分支和二级节点，是未来制作内容，不是泛用模板。根节点使用章节主题。默认展示 4—6 个主分支，较大章节按学习单元分层，避免一次展开几十个节点。

- 点击节点展开概念解释、一个例子、一个常见误区，以及对应原文小节或实验入口。
- 点击“在实验中查看”定位到指定场景与步骤；例如“虚拟节点”直接打开哈希环的虚拟节点对照。
- 支持全览、聚焦、搜索、折叠和面包屑返回；移动端提供同结构的大纲列表。
- 提供记忆模式：隐藏二级节点，用户自己补全后对照；匹配同义表达，不要求背固定句子。
- 连线写出关系：解决、依赖、导致、替代、权衡。架构图描述系统组件，思维导图描述知识关系，不能简单互换。
- “已读”“已实验”“能解释”分别记录；点击过节点不等于掌握。
- 增强项：导出图像或可打印大纲、个人备注与跨章跳转；本次只规划这些能力。

## 讲解与练习

每章至少准备：一段直觉解释、一段逐步机制解释、一张方案比较表、两张误区卡、一张 90 秒复述卡。生活类比后必须写明类比失效之处，例如排队买票无法完整表示分布式一致性。

练习至少包含一题流程排序、一题结果预测、一题故障诊断、一题取舍解释。可按章节特点将流程题换成估算题。答案先隐藏，提交后解释错误选项为什么诱人、错在哪个前提；不要只给对错。

开放题采用四维反馈：需求是否说清、机制是否正确、取舍是否有前提、异常是否考虑。每维以“缺失／部分／清楚”呈现，并提供可比对的参考要点，不声称模拟真实公司的录用评分。

复习默认建议首次学习后第 1、3、7、14 天回顾，可调整或关闭。这是产品初始节奏假设，不宣称对所有人最佳。系统优先选出答错概念及其相邻知识，不只重复题干。没有账户也应能完成学习；跨设备同步作为后续能力。

## 跨章帮助

- 基础入口：HTTP 请求与响应、缓存、数据库索引、事务、同步/异步、分片/复制的微型词典。每项只解释进入当前章所需内容。
- 决策词典：读多写少、热点、顺序、重复、延迟、成本、恢复；列出这些约束在哪些章再次出现。
- 错误驱动导航：重复处理 → 通知／队列／支付；状态过期 → 缓存／聊天／好友；并发写 → KV／预订／钱包。
- 面试模式：给定需求后先隐藏参考架构；完成澄清、估算和高层设计后逐步揭示参考点。不能在用户开始前展示整张答案图。
- 对照笔记：记录“我最初的判断、实验看到的证据、修正后的原则”。导出学习摘要比积分排行榜更优先。

## 内容准确性

正文核对过的章节也不能把书中的示意数字当成现行产品参数。原文、推导、教学假设、进阶补充分别标注。计算统一说明天数、十进制/二进制单位、平均/峰值、逻辑/物理容量及复制开销。

所有实验优先给确定性的样例与不变量。例如成功预订不超库存、重复支付不新增业务扣款、同一日志重放得到相同状态。涉及性能只给明确模型下的趋势，涉及协议语义先声明保证边界。未来制作时若引用具体数据库、协议或供应商行为，应再核对对应版本的官方资料。

不安排真实付款、交易、邮件投递或外网抓取。它们在学习网站中只作为虚构数据与本地事件模型；这是教学产品范围，不是要求用户操作真实服务。
